import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Promptshop - Your Personal AI Prompt Library',
  description: 'Upload images and generate AI prompts with Claude. Save, organize, and manage your prompt collection.',
  keywords: 'AI, prompts, image analysis, Claude, prompt library',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}